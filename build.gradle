plugins {
    id 'java-library'
}

repositories {
        mavenCentral() // https://mvnrepository.com/repos/central - сайт, откуда берем библиотеку selenide

    }

dependencies {
    testImplementation(
            "com.codeborne:selenide:7.12.1" // - последняя версия на сайте mavenCentral, если в поисковике вбить selenide
    )
}